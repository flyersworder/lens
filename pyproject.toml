[project]
name = "lens-research"
version = "0.9.1"
description = "LLM Engineering Navigation System — discovers recurring solution patterns from LLM research papers"
readme = "README.md"
requires-python = ">=3.12"
authors = [{ name = "Qing", email = "qingye779@gmail.com" }]
license = { text = "MIT" }
keywords = ["llm", "research", "tradeoffs", "triz", "knowledge-base", "arxiv", "ai"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "pydantic>=2.0",
    "typer>=0.12",
    "pyyaml>=6.0",
    "openai>=1.0",
    "rich>=13.0",
    "httpx>=0.28.1",
    "tenacity>=9.1.4",
    "sentence-transformers>=5.3.0",
    "sqlite-vec>=0.1.7",
    "arxiv>=2.4.1",
    "python-dotenv>=1.2.2",
    "json-repair>=0.58.7",
]

[project.optional-dependencies]
litellm = ["litellm>=1.40"]
deepxiv = ["deepxiv-sdk>=0.2.5"]

[project.scripts]
lens = "lens.cli:app"

[project.urls]
Homepage = "https://github.com/flyersworder/lens"
Repository = "https://github.com/flyersworder/lens"
Issues = "https://github.com/flyersworder/lens/issues"
Changelog = "https://github.com/flyersworder/lens/releases"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lens"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: marks tests that hit live APIs (deselect with '-m \"not integration\"')",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "ruff>=0.8",
    "pytest-timeout>=2.4.0",
]

[tool.uv]
# Override arxiv's requests~=2.32.0 pin to pick up CVE-2026-25645 fix
override-dependencies = ["requests>=2.33.0"]

[tool.ruff]
target-version = "py312"
src = ["src"]
line-length = 99

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
